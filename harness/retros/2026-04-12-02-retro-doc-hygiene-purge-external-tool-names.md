# 2026-04-12-02 retro doc hygiene purge external tool names

- 做得好的地方：用「删除自指审计包 + 中性化历史表述」两步，避免在模板里长期保留某第三方工具名称，同时不完全销毁历史轮次结构。
- 可改进：若未来仍需保留「当时 exact 命令」，可放到受控的外部归档（不进模板仓库），或统一用占位符规范（仍不出现商业工具名）。
- 下轮建议：在 `AGENTS.md` 或白皮书模板中可选增加一条「模板文档禁止绑定具体商业测试工具名称」的软规范（仅当产品方需要时再做）。
