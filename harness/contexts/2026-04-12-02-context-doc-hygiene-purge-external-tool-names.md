# 2026-04-12-02 context doc hygiene purge external tool names

## 上一轮上下文

- `2026-04-12-01` 已将模板实现路径上的浏览器自动化执行链与 Node 依赖从主路径移除，但审计目录中仍保留若干带第三方工具名称的文档与专用 plan。

## 本轮切换

- 用户要求模板仓库**完全不出现**某一具体浏览器自动化商业工具名称（含历史审计中的命令行片段与文件名）。
- 本轮以「文档卫生 + 中性化表述」为主，不引入新的测试框架或依赖。

## 约束

- 不修改 `task-state.json` 状态机定义（除非质量门禁要求）。
- 不恢复任何已删除的业务样例代码；仅处理文档与索引中的名称残留。
