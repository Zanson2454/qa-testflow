# YYYY-MM-DD-NN plan <short-summary>

status: draft

> 将 `<short-summary>` 换成本轮英文短名；发布本轮时将 `status` 改为 `active`，并在 `docs/plans/index.md` 与 `workflow/state/task-state.json` 的 `current_plan` 中同步。

## goal

- [用一句话说明本轮要达成的目标]

## steps

1. [步骤一] -> [预期产物或状态]
2. [步骤二] -> [预期产物或状态]

## done_criteria

- [ ] [可验收的完成条件 1]
- [ ] [可验收的完成条件 2]
- [ ] `python3 workflow/check_quality.py` 通过
- [ ] 本轮 harness 四件套已落盘且 `review` 结论明确
